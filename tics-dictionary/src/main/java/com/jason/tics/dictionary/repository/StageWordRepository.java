package com.jason.tics.dictionary.repository;

import com.jason.tics.dictionary.domain.extension.StageWord;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * @author Jason
 */
public interface StageWordRepository extends JpaRepository<StageWord, StageWord> {
}
